---
description: Design or verify surface-layer alignment — user-facing interfaces and tech stack shaped by dominant-ops pressure
---

Trigger the ec:align-surface skill. In design mode (no existing code), guide interface design from dominant-ops user journeys and validate tech stack choices against constraints. In verify mode (existing code), audit coverage and capacity. Interfaces include APIs, web UI, CLI, admin panels — whatever the system exposes to its users.
