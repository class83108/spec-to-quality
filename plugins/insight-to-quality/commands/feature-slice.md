---
description: Scope a concrete feature into a shared work card, map it to the global interaction surface, and route to the earliest missing clarification or deep-dive layer before TDD
---

Trigger the feature-slice skill. Map the user idea to goals, the global interaction surface, design drivers, responsibility units, and seams, create or update a shared feature work card, then route to the earliest missing clarification layer (`goals-discovery`, `design-driver-discovery`, `system-map`, `spec-clarification`, or `tdd-ready-check`) or flag a targeted deep dive when the structure is still unstable.
