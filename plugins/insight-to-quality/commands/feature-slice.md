---
description: Legacy command. Prefer `implementation-planning` plus `feature-brief` for the current workflow.
---

Trigger the legacy feature-slice skill only when working with the older work-card-based flow. For the current workflow, prefer `implementation-planning` and then `feature-brief`.
