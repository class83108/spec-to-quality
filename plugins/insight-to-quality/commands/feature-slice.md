---
description: Scope a concrete feature into a shared work card, align it to discovery.md, system-design.md, and SYSTEM_MAP.md, then route to the earliest missing clarification layer before TDD
---

Trigger the feature-slice skill. Map the user idea to discovery.md, system-design.md, and SYSTEM_MAP.md, cut the first implementable slice, create or update a shared feature work card, then route to the earliest missing clarification layer (`discovery`, `system-design`, `system-map`, `spec-clarification`, or `tdd-ready-check`).
