---
description: Scope a new feature and route to the earliest missing layer, then continue to spec-backlog execution
---

Trigger the start-feature skill. Map the user idea to Gx / Dx / SYSTEM_MAP boundaries, then route to the earliest skill that must be filled first (goals, dominant-ops, system-map, align-*). If prerequisites are already in place, go directly to `docs/spec-backlog/index.md` and pick a `ready` item.
