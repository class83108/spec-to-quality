description: Extract acceptance-worthy behavior from a feature slice into a Gherkin .feature file
---

Trigger the gherkin-extraction skill. Read the shared feature work card, decide whether Gherkin is actually warranted, extract only the acceptance-worthy scenarios, and write the `.feature` file when it adds real value.
