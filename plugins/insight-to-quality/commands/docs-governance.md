---
description: Documentation governance skill (small scope, repeatable runs)
---

Trigger the docs-governance skill. Handle one scope (`seam:*` / `finding:*` / `journey:*` / `doc:*`) and one mode (`detect` / `sync` / `route` / `archive-check`) at a time, using a minimal read set to govern document lifecycle and drift.
